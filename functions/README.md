# Implementación anterior — no desplegar

Esta carpeta conserva temporalmente la primera implementación de
`createAutologUser`, `updateAutologUser` y `disableAutologUser` sólo para
comparación.

La lógica necesaria fue modularizada y ampliada en el backend canónico:

`D:\Proyectos\Subida_fire_hosting\autolog\functions`

No ejecutar Firebase CLI ni desplegar Functions desde
`D:\Proyectos\autolog`. Esta carpeta no está referenciada por el
`firebase.json` de la raíz frontend.
